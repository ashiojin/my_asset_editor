info:
  name: "[DEBUG] print_animation_targets"
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3000/api/_debug
  body:
    type: json
    data: '"print_animation_targets"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
